import 'package:flutter/material.dart';

class OpenPositionsC extends StatelessWidget {
  const OpenPositionsC({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
